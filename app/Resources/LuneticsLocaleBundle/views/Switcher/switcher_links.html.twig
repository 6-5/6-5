{% for locale in locales %}
    <li>
        <a class="{% if current_locale == locale.locale %}font-bold{% endif %}"
           href="{{ locale.link }}"
           title="{{ locale.locale_current_language }}"
        >{{ locale.locale|upper }}</a>
    </li>
{% endfor %}