{
  "name": "6-5",
  "scripts": {
    "postdeploy": "php bin/console doctrine:schema:create && php bin/console doctrine:fixtures:load -n"
  },
  "env": {
    "SYMFONY_ENV": "prod"
  },
  "formation": {
  },
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [
    {
      "url": "heroku/php"
    }
  ]
}
